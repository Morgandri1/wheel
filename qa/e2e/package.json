{
  "name": "wheel-qa-e2e",
  "private": true,
  "description": "QA end-to-end suite. Lives outside web/ so QA owns it (contract §1).",
  "scripts": {
    "test": "playwright test",
    "install-browsers": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "1.48.2"
  }
}
